package net.egartley.beyondorigins.engine;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

public class GameItemTest {

    @Test
    void sameItemCompare() {
        // GameItem item1 = new GameItem(...);
        // GameItem item2 = new GameItem(...);

        // boolean sameItem = item1.is(item2);

        // assertTrue(sameItem);
    }

    @Test
    void differentItemCompare() {
        // GameItem item1 = new GameItem(...);
        // GameItem item2 = new GameItem(...);

        // boolean sameItem = item1.is(item2);

        // assertFalse(sameItem);
    }

}
